# Whale Alert API 配置
WHALE_ALERT_API_KEY=your_api_key_here

# WebSocket 订阅配置
# 币种列表（逗号分隔，小写），例如: "btc,eth,xrp,usdt"
# 注意：Whale Alert API 每小时最多接收 100 条警报，建议根据需求选择币种数量
# 支持的币种: btc, eth, sol, avax, matic, usdt, bnb, xrp, ada, dot, link, ltc, bch 等
SYMBOLS=btc,eth,sol,avax,matic,usdt,bnb,xrp

# 区块链列表（逗号分隔，小写），例如: "bitcoin,ethereum,ripple"
# 支持的区块链: bitcoin, ethereum, solana, avalanche, polygon, binance, ripple, tron 等
BLOCKCHAINS=bitcoin,ethereum,solana,avalanche,polygon,binance,ripple,tron

# 最小转账金额（美元），默认 500000（50万美元）
WHALE_ALERT_MIN_VALUE_USD=500000

# Binance API 配置（可选）
BINANCE_API_KEY=your_binance_key_optional
BINANCE_API_SECRET=your_binance_secret_optional

# Redis 配置（优先使用 REDIS_URL）
# 方式1: 使用 URL（推荐）
REDIS_URL=redis://localhost:6379/0
# 如果有密码: redis://:password@localhost:6379/0

# 方式2: 分别配置（向后兼容，如果未设置 REDIS_URL 时使用）
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0
# REDIS_PASSWORD=
